# @package _global_
defaults: 
  - _self_

model:
  name: resnet50
  loss_fun: 'UANLL'
  label_smoothing: 0.0
  checkpoint_path: null
  optimizer_name: Adam
  optimizer_hparams:
    weight_decay: 1e-4


dataset:
  img_size: 32
  bagging_size: 15000

trainer:
  max_epochs: 30
